[tool.uv.workspace]
members = ["packages/*"]

# -- ruff --

[tool.ruff]
target-version = "py313"
line-length = 100
# TODO: remove once tests/ moves out of src/ in rbtr-legacy
src = ["packages/rbtr-legacy/src"]

[tool.ruff.format]
quote-style = "double"

[tool.ruff.lint]
select = [
  "A",
  "B",
  "C4",
  "DTZ",
  "E",
  "F",
  "G",
  "I",
  "LOG",
  "PT",
  "RUF",
  "S",
  "SIM",
  "UP",
  "W",
]
ignore = ["E501", "W191"]
unfixable = ["B"]

[tool.ruff.lint.extend-per-file-ignores]
"packages/*/src/tests/**/*.py" = [
  "S101", # asserts allowed in tests
  "S105", # hardcoded password in test assertions
  "S106", # hardcoded password in test arguments
  "S107", # hardcoded password in test defaults
]

[tool.ruff.lint.flake8-quotes]
docstring-quotes = "double"

[tool.ruff.lint.isort]
known-first-party = ["rbtr"]
combine-as-imports = true

[tool.ruff.lint.mccabe]
max-complexity = 12

# -- rumdl --

[tool.rumdl]
respect_gitignore = true
exclude = ["cov-append.md", "TODO-*"]
extend-enable = ["MD060"]

[tool.rumdl.MD013]
line_length = 88
headings = false
code_blocks = false
tables = false
reflow = true

[tool.rumdl.MD060]
style = "aligned"

# -- sqlfluff --

[tool.sqlfluff.core]
dialect = "duckdb"
templater = "placeholder"
sql_file_exts = ".sql"

[tool.sqlfluff.indentation]
indent_unit = "space"
tab_space_size = 2

[tool.sqlfluff.templater.placeholder]
param_style = "question_mark"

# -- mypy --

[tool.mypy]
python_version = "3.13"
mypy_path = "packages/rbtr-legacy/src"
packages = ["rbtr"]
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_defs = true
follow_imports = "normal"
show_error_codes = true
warn_redundant_casts = true
warn_unused_configs = true

[[tool.mypy.overrides]]
module = "frontmatter"
ignore_missing_imports = true

# -- Pytest & Coverage --

[tool.pytest.ini_options]
testpaths = ["packages/rbtr-legacy/src/tests"]
minversion = "6.0"
addopts = ["--strict-markers", "--import-mode=importlib", "-n", "auto"]

[tool.coverage.run]
branch = true

[tool.coverage.report]
show_missing = true

[tool.vulture]
min_confidence = 80
paths = ["packages/rbtr-legacy/src/rbtr"]
ignore_names = [
  "init_settings",        # pydantic_settings settings_customise_sources
  "dotenv_settings",      # pydantic_settings settings_customise_sources
  "file_secret_settings", # pydantic_settings settings_customise_sources
  "exc_type",             # __exit__ signature
  "exc_val",              # __exit__ signature
  "exc_tb",               # __exit__ signature
  "user_data",            # llama_cpp log callback signature
  "format",               # http.server log_message signature
]
