[tool.uv.workspace]
members = ["packages/rbtr", "packages/rbtr-legacy", "packages/rbtr-eval"]

# -- ruff --

[tool.ruff]
target-version = "py313"
line-length = 100
# TODO: remove once tests/ moves out of src/ in rbtr-legacy
src = ["packages/rbtr/src", "packages/rbtr-legacy/src", "packages/rbtr-eval/src"]

[tool.ruff.format]
quote-style = "double"

[tool.ruff.lint]
select = [
  "A",
  "B",
  "C4",
  "DTZ",
  "E",
  "F",
  "G",
  "I",
  "LOG",
  "PT",
  "RUF",
  "S",
  "SIM",
  "UP",
  "W",
]
ignore = ["E501", "W191"]
unfixable = ["B"]

[tool.ruff.lint.extend-per-file-ignores]
"packages/*/src/tests/**/*.py" = [
  "S101", # asserts allowed in tests
  "S105", # hardcoded password in test assertions
  "S106", # hardcoded password in test arguments
  "S107", # hardcoded password in test defaults
]

[tool.ruff.lint.flake8-quotes]
docstring-quotes = "double"

[tool.ruff.lint.isort]
known-first-party = ["rbtr", "rbtr_legacy"]
combine-as-imports = true

[tool.ruff.lint.mccabe]
max-complexity = 12

# -- rumdl --

[tool.rumdl]
respect_gitignore = true
exclude = ["cov-append.md", "TODO-*"]
extend-enable = ["MD060"]

[tool.rumdl.MD013]
line_length = 88
headings = false
code_blocks = false
tables = false
reflow = true

[tool.rumdl.MD060]
style = "aligned"

# -- sqlfluff --

[tool.sqlfluff.core]
dialect = "duckdb"
templater = "placeholder"
sql_file_exts = ".sql"

[tool.sqlfluff.indentation]
indent_unit = "space"
tab_space_size = 2

[tool.sqlfluff.templater.placeholder]
param_style = "question_mark"

# -- mypy --

[tool.mypy]
python_version = "3.13"
mypy_path = "packages/rbtr/src:packages/rbtr-legacy/src:packages/rbtr-eval/src"
packages = ["rbtr", "rbtr_legacy", "rbtr_eval"]
disallow_incomplete_defs = true
disallow_untyped_calls = true
disallow_untyped_defs = true
follow_imports = "normal"
show_error_codes = true
warn_redundant_casts = true
warn_unused_configs = true

[[tool.mypy.overrides]]
module = "frontmatter"
ignore_missing_imports = true

# -- Pytest & Coverage --

[tool.pytest.ini_options]
testpaths = ["packages/rbtr/src/tests", "packages/rbtr-eval/src/tests"]
minversion = "6.0"
addopts = ["--strict-markers", "--import-mode=importlib", "-n", "auto"]

[tool.coverage.run]
branch = true

[tool.coverage.report]
show_missing = true

# -- Versioning --

[tool.bumpversion]
current_version = "2026.4.1-dev0"
parse = """(?x)                     # Verbose mode
    (?P<release>                    # The release part
        (?:[1-9][0-9]{3})\\.        # YYYY.
        (?:1[0-2]|[1-9])            # MM.
    )
    \\.(?P<num>0|[1-9]\\d*)         # num version number
    (?:
         -(?P<pre_label>dev|stable)     # pre_release label
        (?P<pre_release>0|[1-9]\\d*)    # pre_release version number
    )?
"""
serialize = ["{release}.{num}-{pre_label}{pre_release}", "{release}.{num}"]
search = "{current_version}"
replace = "{new_version}"

[tool.bumpversion.parts.release]
calver_format = "{YYYY}.{MM}"

[tool.bumpversion.parts.num]
independent = false

[tool.bumpversion.parts.pre_label]
optional_value = "stable"
values = ["dev", "stable"]

[[tool.bumpversion.files]]
glob = "packages/*/pyproject.toml"

[[tool.bumpversion.files]]
glob = "packages/*/package.json"
search = '"version": "{current_version}"'
replace = '"version": "{new_version}"'

[tool.vulture]
min_confidence = 80
paths = ["packages/rbtr/src/rbtr", "packages/rbtr-legacy/src/rbtr_legacy"]
ignore_names = [
  "init_settings",        # pydantic_settings settings_customise_sources
  "dotenv_settings",      # pydantic_settings settings_customise_sources
  "file_secret_settings", # pydantic_settings settings_customise_sources
  "exc_type",             # __exit__ signature
  "exc_val",              # __exit__ signature
  "exc_tb",               # __exit__ signature
  "user_data",            # llama_cpp log callback signature
  "format",               # http.server log_message signature
]
